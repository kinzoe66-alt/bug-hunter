# Specs

Contains example application specifications:

- `api_security.appspec.yaml` - Standard API security specification.
- `api_security_failure.appspec.yaml` - Example specification for failure/degraded testing.

