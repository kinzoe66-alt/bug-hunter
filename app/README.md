# App

User-facing entry points:

- **main.py** - Standard execution of AppSpec.
- **main_failure.py** - Executes a deliberately failing appspec for testing.
- **replay_latest.py** - Validates deterministic replay of a previous execution.

